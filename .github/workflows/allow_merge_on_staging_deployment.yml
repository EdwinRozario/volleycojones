name: Staging deployment check

on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize]

jobs:
  prevent_merge:
    if: 
    name: Prevent Merge
    runs-on: ubuntu-latest
    steps:
      - name: Check for doc label
        run: |
          if [[ "${{ github.event.pull_request.labels.*.name }}" =~ "documentation"]]; then
            exit 0
          else
            exit 1
          fi
